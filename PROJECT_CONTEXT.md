# Token Profiler — سند زمینه و مشخصات پروژه

> این فایل حاصل یک جلسه‌ی طولانی ایده‌پردازی و تصمیم‌گیریه.
> هدفش اینه که Claude Code بدون شروع از صفر، دقیقاً از همین نقطه ادامه بده.
> قبل از نوشتن کد، این فایل رو کامل بخون.

---

## ۱. زمینه‌ی توسعه‌دهنده

- ویکتور، توسعه‌دهنده‌ی React، در مسیر full-stack شدن.
- تجربه‌ی قبلی در AI/ML ندارد؛ اما با ادغام LLM در اپلیکیشن آشناست.
- بودجه: صفر. سرور اختصاصی ندارد. هزینه‌ی ماهانه‌ی API باید حداقل بماند.
- زبان ارتباطی: فارسی و انگلیسی، هر دو.

**سبک کار مورد توافق:** Claude کد را می‌نویسد، ویکتور بازبینی می‌کند و هر جای مبهم را می‌پرسد.
هدف این است که ویکتور کد را بفهمد، نه فقط تحویل بگیرد. پس هر تصمیم غیربدیهی باید کوتاه توضیح داده شود.

---

## ۲. این پروژه چیست

یک **Token Profiler** برای توسعه‌دهندگانی که با LLM و agent کار می‌کنند.

نه یک داشبورد لاگ. یک **پروفایلر**.

تفاوت مهم است: ابزارهای موجود (Langfuse، Helicone و مشابه) عمدتاً **گزارش‌گر**اند —
لاگ را نشان می‌دهند و تمام. کاری که نمی‌کنند این است که بگویند:

- «۴۰٪ توکن این prompt هدر می‌رود چون context تکراری می‌فرستی»
- «این tool در ۸۰٪ موارد بی‌فایده صدا زده می‌شود»
- «از کل مصرف این run، این‌قدر system prompt بود، این‌قدر تاریخچه، این‌قدر thinking»

**این تحلیل، قلب محصول است — نه یک feature جانبی.**

---

## ۳. تصمیم‌های قطعی‌شده

| موضوع | تصمیم | دلیل |
|---|---|---|
| زبان | **TypeScript** | ویکتور React-کار است؛ زمان یادگیری صفر. مسیر full-stack هم همین است. اکوسیستم agent در TS کمتر اشباع شده. |
| Python | **فعلاً نه** | بعداً یک client نازک Python که به همان پروتکل وصل شود. حدود یک هفته کار. |
| رابط | **CLI + Local Web UI** | ورود ترمینالی، نمایش مرورگری. |
| میزبانی | **Local-first، بدون سرور** | هزینه صفر. مانع پذیرش صفر. توسعه‌دهنده‌ها به ابزاری که داده‌شان را جایی بفرستد بدبین‌اند. |
| ذخیره‌سازی | **SQLite** | داده‌ی درختی است؛ کوئری زدن روی فایل JSON خیلی زود دردسر می‌شود. |
| Provider اول | **Anthropic API** | اما interface باید از روز اول provider-agnostic طراحی شود. |
| مجوز | **Open source** | این repo قرار است رزومه‌ی فنی و ویترین جهانی ویکتور باشد. |

### تجربه‌ی مورد نظر کاربر

```bash
npx token-profiler
```

سرور محلی بالا می‌آید، مرورگر باز می‌شود.
بدون ثبت‌نام. بدون حساب کاربری. بدون اینکه یک بایت داده جایی برود.

علاوه بر آن، یک خروجی خلاصه‌ی ترمینالی هم لازم است — یک جدول کوچک:
۱۰ اجرای اخیر، مصرف، هزینه‌ی تخمینی.

---

## ۴. محدوده‌ی MVP

### داخل محدوده

**الف) کتابخانه‌ی سبک (wrapper)**

دور فراخوانی مدل می‌پیچد و این‌ها را ثبت می‌کند:

- input tokens / output tokens
- thinking tokens (اگر مدل جدا گزارش می‌دهد)
- cache read / cache write
- هر tool call با توکن مصرفی خودش
- مدت زمان
- هزینه‌ی تخمینی

الزام مهم: **باید async و non-blocking باشد.** نباید اجرای کاربر را کند کند.
اگر ثبت لاگ شکست خورد، نباید برنامه‌ی کاربر را بشکند.

**ب) ساختار درختی — مهم‌ترین تصمیم طراحی**

یک agent run یک **درخت** است:

```
run
 └─ step
     └─ tool call
         └─ nested call
```

**اگر از اول flat ذخیره شود، بعداً قابل اصلاح نیست.**

هر رکورد باید داشته باشد:
- `id`
- `parent_id`
- `root_id`
- `type` (run / step / llm_call / tool_call)
- `started_at` / `ended_at`
- `token_breakdown` (شکسته به اجزا)
- `metadata` (نام مدل، نام ابزار، نسخه‌ی prompt)

**ج) سه تحلیل که تمایز محصول را می‌سازند**

1. **تفکیک هزینه (Cost Breakdown)**
   از کل توکن یک run: چند درصد system prompt، چند درصد conversation history،
   چند درصد tool output، چند درصد thinking.
   *معمولاً توسعه‌دهنده وقتی این نسبت را می‌بیند شوکه می‌شود — این همان لحظه‌ی «aha» محصول است.*

2. **تشخیص هدررفت (Waste Detection)**
   - context تکراری که هر بار دوباره فرستاده می‌شود
   - خروجی tool که هرگز استفاده نشد
   - retry ها و هزینه‌شان

3. **مقایسه‌ی نسخه‌ها (Version Diff)**
   prompt عوض شد → مصرف و رفتار چطور تغییر کرد.

**د) رابط وب ساده**
- نمای درختی run
- نمودار تفکیک هزینه
- بدون auth، بدون multi-tenant

### خارج از محدوده (فعلاً ننویس)

- احراز هویت و حساب کاربری
- حالت ابری / hosted
- multi-tenant
- ارزیابی کیفیت خروجی (eval) — این فاز بعدی است
- پشتیبانی از چند provider — فقط interface را آماده نگه دار

---

## ۵. معماری

```
[کتابخانه‌ی کلاینت (TS)]
   │  async, non-blocking
   ▼
[صف داخلی / buffer]
   │
   ▼
[لایه‌ی ذخیره‌سازی → SQLite]
   │
   ▼
[لایه‌ی تجمیع و تحلیل]
   │
   ▼
[CLI]  +  [Local Web UI]
```

همه‌چیز داخل یک بسته‌ی npm:
- به‌عنوان library قابل import
- به‌عنوان CLI قابل اجرا با `npx`
- Web UI هم داخل همان بسته و local serve می‌شود

---

## ۶. اولین قدم — از این‌جا شروع کن

خیلی کوچک شروع کن. هدف قدم اول: **فقط جمع کردن داده.**

1. تعریف تایپ‌های داده (`RunNode`, `TokenBreakdown`)
2. یک تابع wrapper دور فراخوانی Anthropic API
3. نوشتن رویدادها در SQLite
4. یک اسکریپت نمونه که چند فراخوانی بزند و نتیجه ثبت شود

**بدون UI. بدون CLI زیبا. فقط داده.**

وقتی این کار کرد، با نگاه کردن به داده‌ی واقعی می‌فهمیم چه تحلیلی واقعاً مفید است —
و همان تحلیل‌ها می‌شوند محصول نهایی.

---

## ۷. ریسک‌ها و نکات مهم

- **رقابت واقعی است.** Langfuse و Helicone وجود دارند و بالغ‌اند.
  تمایز فقط و فقط در لایه‌ی **تحلیل و توصیه** است، نه در جمع‌آوری لاگ.
  اگر این پروژه به یک «داشبورد لاگ دیگر» تبدیل شود، شکست خورده است.

- **هزینه‌ی توسعه را پایین نگه دار.** موقع توسعه، پاسخ‌های API را mock کن.
  فقط برای تست رفتار واقعی به API واقعی وصل شو. از ارزان‌ترین مدل استفاده کن.

- **ریسک رها شدن پروژه.** پروژه‌ی قبلی (JP) رها شد. این پروژه عمداً کوچک انتخاب شده
  تا در ۲ تا ۳ هفته به یک نسخه‌ی قابل انتشار برسد. **از scope creep جداً پرهیز کن.**

---

## ۸. تصویر بلندمدت

این پروژه دو نقش دارد:

1. **رزومه‌ی فنی** — یک repo عمومی که مهارت‌های واقعی را نشان می‌دهد
2. **تمرین مسیر full-stack** — صف، worker، طراحی API، ادغام LLM، ذخیره‌سازی، UI

درآمدزایی هدف اولیه نیست. اگر بعداً محصول جواب داد، مسیرش باز است.